@model Create.Models.Team
@{
    ViewBag.Title = "AddTeam";
}

<link href="~/Content/bootstrap.css" rel="stylesheet" />
<body>
    <header>
        <nav class="navbar navbar-default">
            <div class="container-fluid">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="@Url.Action("Index", "Home")">CREATE</a>
                </div>

                <ul class="nav navbar-nav navbar-right">

                    <li><a href="@Url.Action("AboutUs", "Home")">About Us</a></li>
                    <li><a href="@Url.Action("CreateUser", "User")">Join Us</a></li>
                    <li><a href="@Url.Action("Login", "Account")">Login</a></li>
                </ul>
            </div>
        </nav>
        
        <br /><br /><br /><br /><br /><br /><br /><br />
        <center>
            <h4>Add New User</h4>
            @using (Html.BeginForm())
            {
                <table>
                    <tr>
                        <td>@Html.Label("teamname")</td>
                        <td>@Html.TextBox("txtteamName")</td>
                    </tr>
                    <tr>
                        <td>  @Html.Label("user")</td>
                        <td>@Html.TextBox("txtuser") </td>
                    </tr>

                </table>

                <div>
                    <br />
                    <input type="submit" value="Log in" />
                </div>
                
                <br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br />
            }

        </center>
    </header>
</body>
