@*@model IEnumerable<Create.Models.Users>*@
@model Create.Models.Users
@{
    ViewBag.Title = "TeamFriend";
}

<title>@Session["teamNamee"]</title>

<table class="table table-striped table-hover ">
    <thead>
        <tr>
            <th>ID</th>
            <th>User Name</th>
            <th>Email</th>
        </tr>
    </thead>
    <tbody>
        @*@foreach (var item in Model)
        {
            @*<tr class="active">
                <td>@item._id</td>
                <td>@item.UserName</td>
                <td>@item.Email</td>*@
                
                <td>@Model._id</td>
                <td>@Model.UserName</td>
                <td>@Model.Email</td>
            </tr>
        @*}*@
    </tbody>
</table>